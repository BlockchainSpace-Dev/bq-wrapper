import { insert } from "../../src/functions/index.js";
import { BigQuery } from "@google-cloud/bigquery";

describe("insert wrapper functions", () => {
  it("should throw error when data not object or not have value", () => {});
});
